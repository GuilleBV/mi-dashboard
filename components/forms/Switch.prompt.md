iOS-style toggle for settings and habit/supplement preferences. Off shows a recessed zinc track; on fills electric-blue with the active glow and springs the knob across.

```jsx
<Switch checked={notif} onChange={setNotif} ariaLabel="Notificaciones" />
<Switch checked size="sm" />
```
