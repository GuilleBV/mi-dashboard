This is my existing React + Vite dashboard, already connected to Supabase and deployed on Vercel. Do NOT change any config, environment variables, deploy settings, package dependencies, or my Supabase schema unless I explicitly ask.

Step 1: Reset the main page (e.g. App.jsx) to a clean blank starting point. Delete the demo/placeholder components that are there now.

Step 2: Here's what I'm building:
[ WRITE YOUR IDEA HERE — what the app is for + the 2-3 main things the main page should show ]

Rules:
- Build ONLY what I listed above. No extra cards, no fake/demo data, no features I didn't ask for.
- One clean page. Simple layout, most important thing biggest and at the top.
- Pull real data from Supabase where relevant. If a table doesn't exist yet, tell me what to create — don't invent data.

Before writing any code, show me a short plan: which files you'll touch and the layout. Wait for my "go," then build one piece at a time so I can check each step.
