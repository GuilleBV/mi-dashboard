Push this folder to GitHub for me automatically — no manual steps.

Use the folder I currently have open. Assume I'm logged into the `gh` CLI
(if I'm not, run `gh auth login` and walk me through it once).

Do this end to end:
1. git init, add all files, and commit.
2. Create a GitHub repo named after this folder and push to it in one step:
   gh repo create --public --source=. --remote=origin --push
3. Print my new GitHub repo URL when done.

From now on, when I say "push my changes", automatically run
git add + commit + push so GitHub stays up to date. Don't make me do it manually.

